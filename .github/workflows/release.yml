name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: CSKU-Lab/.github/.github/workflows/release.yml@main
    with:
      docker_workflow: docker-publish.yml
    secrets: inherit
